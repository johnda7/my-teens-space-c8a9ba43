# 🚀 Руководство по запуску MyTeens.Space v2.0

## 📋 Что было добавлено

### Backend улучшения:
- ✅ Система аутентификации с кодами доступа
- ✅ Роли пользователей (студент/родитель/куратор)
- ✅ API для кураторов (генерация кодов, просмотр учеников)
- ✅ Система достижений и уведомлений
- ✅ Подсчет стриков и уровней
- ✅ Расширенная статистика прогресса
- ✅ API для родительского контроля

### Frontend улучшения:
- ✅ Страница входа с кодом доступа
- ✅ Панель куратора с мониторингом учеников
- ✅ Защищенные маршруты по ролям
- ✅ Визуализация прогресса учеников
- ✅ Интеграция с новым API

## 🛠 Требования

- **Python 3.10+**
- **Node.js 18+**
- **MongoDB 4.4+** (запущенный локально или удаленно)

## 📦 Установка

### 1. Backend

```bash
cd backend

# Создать виртуальное окружение (опционально)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Проверить .env файл
cat .env
# Должно быть:
# MONGO_URL="mongodb://localhost:27017"
# DB_NAME="myteens_space"
# CORS_ORIGINS="http://localhost:5173,http://localhost:3000,*"
```

### 2. Frontend

```bash
cd frontend

# Установить зависимости (используйте npm, yarn или bun)
npm install
# или
yarn install
# или
bun install

# Проверить .env файл
cat .env
# Должно содержать:
# VITE_API_URL=http://localhost:8000
```

### 3. MongoDB

Убедитесь, что MongoDB запущен:

```bash
# Проверка статуса MongoDB
sudo systemctl status mongodb  # Linux
brew services list | grep mongodb  # Mac

# Или проверьте подключение
mongosh
```

Если MongoDB не установлен:
- **Ubuntu/Debian**: `sudo apt-get install mongodb`
- **Mac**: `brew install mongodb-community`
- **Windows**: Скачайте с [mongodb.com](https://www.mongodb.com/try/download/community)

## 🚀 Запуск

### Вариант 1: Два терминала

**Терминал 1 - Backend:**
```bash
cd backend
uvicorn server:app --reload --port 8000
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
# или
yarn dev
# или
bun dev
```

### Вариант 2: Один терминал (tmux/screen)

```bash
# Запуск backend в фоне
cd backend
uvicorn server:app --reload --port 8000 &

# Запуск frontend
cd ../frontend
npm run dev
```

## 🔑 Первый запуск - Создание куратора

После запуска обоих серверов:

### 1. Создайте куратора через API

```bash
curl -X POST "http://localhost:8000/api/auth/create-curator" \
  -H "Content-Type: application/json" \
  -d '{"name": "Главный Куратор", "age": 30}'
```

Ответ будет содержать:
```json
{
  "curator": {...},
  "access_code": "ABC12345",
  "message": "Куратор создан..."
}
```

### 2. Войдите используя полученный код

1. Откройте `http://localhost:5173/login`
2. Введите код (например, `ABC123`)
3. Вы будете перенаправлены на панель куратора

### 3. Добавьте учеников

На панели куратора:
1. Нажмите "Добавить ученика"
2. Введите имя и возраст
3. Получите код для ученика
4. Отправьте код ученику

### 4. Ученик входит в систему

1. Ученик открывает `http://localhost:5173/login`
2. Вводит свой код
3. Начинает обучение!

## 📚 Основные URL

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs (Swagger)
- **Login**: http://localhost:5173/login
- **Curator Dashboard**: http://localhost:5173/curator
- **Parent Dashboard**: http://localhost:5173/parent

## 🔧 API Endpoints

### Аутентификация
- `POST /api/auth/login?code=ABC123` - Вход по коду
- `POST /api/auth/create-curator` - Создать куратора

### Куратор
- `POST /api/curator/generate-code` - Создать код для ученика
- `GET /api/curator/{curator_id}/students` - Список учеников
- `GET /api/curator/{curator_id}/codes` - Список кодов

### Прогресс
- `POST /api/progress/lesson/{lesson_id}/start` - Начать урок
- `POST /api/progress/lesson/{lesson_id}/complete` - Завершить урок
- `GET /api/progress/{user_id}` - Весь прогресс
- `GET /api/progress/{user_id}/stats` - Статистика

### Колесо баланса
- `POST /api/balance-assessment` - Сохранить оценку
- `GET /api/balance-assessment/{user_id}` - История оценок
- `GET /api/balance-assessment/{user_id}/latest` - Последняя оценка

### Уведомления
- `GET /api/notifications/{user_id}` - Получить уведомления
- `PUT /api/notifications/{notification_id}/read` - Отметить прочитанным

## 🧪 Тестирование

### Создание тестовых данных

```bash
# Создать куратора
curl -X POST "http://localhost:8000/api/auth/create-curator" \
  -H "Content-Type: application/json" \
  -d '{"name": "Тест Куратор", "age": 30}'

# Создать код для ученика (используйте curator_id из предыдущего ответа)
curl -X POST "http://localhost:8000/api/curator/generate-code" \
  -H "Content-Type: application/json" \
  -d '{
    "curator_id": "your-curator-id",
    "student_name": "Тест Ученик",
    "student_age": 14
  }'
```

## ⚠️ Troubleshooting

### MongoDB не подключается
```bash
# Проверьте статус
sudo systemctl status mongodb

# Перезапустите
sudo systemctl restart mongodb

# Проверьте порт
netstat -an | grep 27017
```

### Backend ошибки
```bash
# Проверьте логи
cd backend
python -c "from motor.motor_asyncio import AsyncIOMotorClient; print('Motor OK')"

# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Frontend не подключается к Backend
1. Проверьте `.env` файл в `frontend/`
2. Убедитесь, что `VITE_API_URL=http://localhost:8000`
3. Перезапустите dev сервер

### Ошибка CORS
В `backend/.env` убедитесь:
```
CORS_ORIGINS="http://localhost:5173,http://localhost:3000,*"
```

## 📊 Структура данных MongoDB

После запуска в БД `myteens_space` будут коллекции:
- `users` - Пользователи (ученики, родители, кураторы)
- `access_codes` - Коды доступа
- `lesson_progress` - Прогресс уроков
- `balance_assessments` - Оценки колеса баланса
- `notifications` - Уведомления
- `checkins` - Ежедневные чек-ины

## 🎯 Workflow использования

1. **Куратор создается** через API
2. **Куратор входит** на `/curator` используя свой код
3. **Куратор создает коды** для учеников
4. **Ученики входят** используя свои коды
5. **Ученики проходят уроки**, получают XP, уровни
6. **Куратор мониторит** прогресс всех учеников
7. **Родители могут получить коды** для просмотра своего ребенка

## 🔐 Безопасность

⚠️ **Для продакшена:**
1. Измените `SECRET_KEY` в `backend/.env`
2. Настройте правильные CORS origins
3. Используйте HTTPS
4. Настройте аутентификацию JWT
5. Ограничьте доступ к MongoDB

## 📝 Следующие шаги

- [ ] Добавить экспорт отчетов (PDF/Excel)
- [ ] Реализовать real-time уведомления (WebSockets)
- [ ] Добавить email уведомления
- [ ] Интеграция с Telegram
- [ ] Мобильное приложение
- [ ] Расширенная аналитика

## 💡 Полезные команды

```bash
# Просмотр БД
mongosh myteens_space
db.users.find().pretty()
db.access_codes.find().pretty()

# Очистка БД (осторожно!)
mongosh myteens_space --eval "db.dropDatabase()"

# Проверка логов backend
cd backend
uvicorn server:app --reload --log-level debug

# Сборка frontend для продакшена
cd frontend
npm run build
```

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи backend и frontend
2. Убедитесь, что MongoDB запущен
3. Проверьте .env файлы
4. Перезапустите оба сервера

---

**Готово! 🎉 Платформа MyTeens.Space v2.0 готова к использованию!**
