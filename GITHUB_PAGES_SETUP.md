# 🚀 Настройка GitHub Pages для MyTeens.Space

## 📋 Шаг 1: Настройка репозитория

### 1.1 Активация GitHub Pages
1. Откройте репозиторий на GitHub: `https://github.com/johnda7/my-teens-space-c8a9ba43`
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите:
   - Source: **GitHub Actions** (уже настроено через `.github/workflows/deploy.yml`)

## 🌐 Шаг 2: Настройка кастомного домена

### Вариант A: Использование кастомного домена (например, myteens.space)

#### 2.1 Создайте CNAME файл
Создайте файл `frontend/public/CNAME` с содержимым:
```
myteens.space
```
или
```
app.myteens.space
```

#### 2.2 Настройка DNS у вашего регистратора

**Для основного домена (myteens.space):**
Добавьте следующие A-записи:
```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

**Для поддомена (app.myteens.space):**
Добавьте CNAME запись:
```
CNAME: app → johnda7.github.io
```

#### 2.3 В настройках GitHub Pages
1. Откройте **Settings** → **Pages**
2. В поле **Custom domain** введите ваш домен: `myteens.space`
3. Дождитесь проверки DNS (может занять до 24 часов)
4. ✅ Включите **Enforce HTTPS** после проверки

### Вариант B: Использование стандартного GitHub Pages URL

Если вы используете стандартный URL `johnda7.github.io/my-teens-space-c8a9ba43`:

1. В файле `frontend/vite.config.ts` установите:
```typescript
base: '/my-teens-space-c8a9ba43/',
```

2. CNAME файл **НЕ НУЖЕН**

## 🔧 Шаг 3: Деплой

### 3.1 Автоматический деплой через GitHub Actions

После пуша в `main` ветку автоматически запустится workflow:

```bash
git add .
git commit -m "Configure for GitHub Pages deployment"
git push origin main
```

### 3.2 Проверка деплоя

1. Перейдите в **Actions** на GitHub
2. Дождитесь завершения workflow "Deploy to GitHub Pages"
3. Проверьте сайт:
   - С кастомным доменом: `https://myteens.space`
   - Без кастомного домена: `https://johnda7.github.io/my-teens-space-c8a9ba43`

## ⚠️ Важные замечания

### Для работы с бэкендом

GitHub Pages хостит только статические файлы (фронтенд). Для бэкенда необходимо:

1. **Развернуть бэкенд отдельно** на:
   - Railway.app (бесплатно для начала)
   - Render.com (бесплатный tier)
   - Heroku
   - DigitalOcean
   - Или любой другой хостинг с поддержкой Python

2. **Обновить API URL в коде** (пока захардкожен `localhost:8001`):
   - Создайте `.env` файл или конфиг с URL бэкенда
   - Обновите все API вызовы для использования production URL

### Environment Variables для GitHub Actions

В **Settings** → **Secrets and variables** → **Actions** добавьте:

```
VITE_API_URL = https://your-backend-url.com/api
```

Это позволит билду использовать правильный URL бэкенда.

## 🎯 Быстрая проверка

После деплоя проверьте:
- ✅ Главная страница загружается
- ✅ Навигация между страницами работает
- ✅ Стили применяются корректно
- ✅ HTTPS работает (если используете кастомный домен)
- ⚠️ API запросы **НЕ БУДУТ** работать до развертывания бэкенда

## 📚 Полезные ссылки

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Configuring a custom domain for GitHub Pages](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)
- [Vite deployment on GitHub Pages](https://vitejs.dev/guide/static-deploy.html#github-pages)

## 🐛 Troubleshooting

### Проблема: 404 на роутах
**Решение:** Добавьте `404.html` равный `index.html` для SPA роутинга:
```bash
cp frontend/dist/index.html frontend/dist/404.html
```

### Проблема: CSS не загружается
**Решение:** Проверьте `base` в `vite.config.ts` - должен совпадать с вашим путем

### Проблема: API запросы не работают
**Решение:** Разверните бэкенд отдельно и обновите `VITE_API_URL`
