# 🎯 Real World Scenarios - Документация

**Дата создания:** 15 ноября 2025  
**Компонент:** `RealWorldScenario.tsx`  
**Линий кода:** 250+

## 📝 Описание

Интерактивный компонент для практики применения навыков в реальных жизненных ситуациях. Подросток видит реалистичный сценарий, выбирает реакцию, получает feedback и узнаёт последствия своего выбора.

## 🎨 Дизайн

**Стиль:** Glassmorphism с мягкими градиентами

**Цветовая палитра:**
- **Здоровые выборы:** Emerald (от 50 до 800)
- **Нездоровые выборы:** Rose (от 50 до 800)
- **Контекст:** Purple-Pink gradient
- **Кнопки выбора:** White/70 с hover эффектом

## 🏗️ Структура компонента

### Props Interface:
```typescript
interface RealWorldScenarioProps {
  title: string;           // Заголовок сценария
  context: string;         // Краткий контекст
  situation: string;       // Детальное описание ситуации
  choices: Choice[];       // Варианты ответов (2-5)
  onComplete?: (data) => void;  // Callback при завершении
}

interface Choice {
  id: string;
  text: string;            // Текст выбора
  isHealthy: boolean;      // Здоровая ли это реакция
  feedback: string;        // Объяснение выбора
  consequence: string;     // Что произойдёт дальше
  skillsUsed: string[];    // Какие навыки применяются
}
```

## 🎯 Функциональность

### 1. Показ ситуации
- Карточка с контекстом (purple-pink gradient header)
- Детальное описание ситуации в отдельном блоке
- Иконка Lightbulb для "подумай"

### 2. Выбор ответа
- 2-5 вариантов ответов в виде кнопок
- Буквенная маркировка (A, B, C, D)
- Hover эффект с увеличением
- Glassmorphism стиль

### 3. Показ результата
- **Если здоровый выбор:**
  - ✨ Emerald градиент
  - CheckCircle иконка
  - "Отличный выбор!" заголовок
  
- **Если нездоровый:**
  - 💭 Rose градиент
  - AlertCircle иконка
  - "Есть вариант получше" заголовок
  - Кнопка "Попробовать другой вариант"

### 4. Последствия
- Белая карточка внутри результата
- Описание "Что произойдёт дальше"
- Помогает понять долгосрочный эффект

### 5. Развитые навыки
- Бейджи с названиями навыков
- Emerald для здоровых выборов
- Blue для нездоровых (что нужно развить)

## 📖 Пример использования

### В allLessonsData.ts:
```typescript
{
  id: 'b1-1-q8',
  type: 'real-world-scenario',
  title: '🎯 Реальная ситуация',
  question: 'Применение границ в жизни',
  context: 'Практика реальных ситуаций',
  situation: 'Твой лучший друг постоянно приходит без предупреждения...',
  scenarioChoices: [
    {
      id: 'choice-1',
      text: 'Промолчать и ждать, пока он сам уйдет',
      isHealthy: false,
      feedback: 'Подавление эмоций вредит тебе...',
      consequence: 'Ты остаешься злым, не делаешь свои дела...',
      skillsUsed: ['Терпение', 'Самоконтроль']
    },
    {
      id: 'choice-2',
      text: 'Сказать: "Мне нужно заняться делами. Давай завтра?"',
      isHealthy: true,
      feedback: 'Отличный выбор! Ты честен и спокоен.',
      consequence: 'Друг понимает, извиняется. Завтра предупредит.',
      skillsUsed: ['Установка границ', 'Честная коммуникация']
    }
  ],
  xpReward: 60
}
```

## 🎨 Анимации

### При появлении:
```typescript
// Context card
initial={{ opacity: 0, y: 20 }}
animate={{ opacity: 1, y: 0 }}

// Choices (staggered)
initial={{ opacity: 0, x: -20 }}
animate={{ opacity: 1, x: 0 }}
transition={{ delay: 0.1 * index }}

// Result появление
initial={{ opacity: 0, scale: 0.95 }}
animate={{ opacity: 1, scale: 1 }}
transition: spring (stiffness: 400, damping: 17)
```

### Hover:
```typescript
whileHover={{ scale: 1.02, y: -2 }}
whileTap={{ scale: 0.98 }}
```

## 💡 Педагогическая ценность

### Что учит компонент:
1. **Критическое мышление** - анализ ситуации перед действием
2. **Последствия выборов** - понимание долгосрочных эффектов
3. **Здоровые vs нездоровые паттерны** - распознавание
4. **Навыки коммуникации** - практика формулировок
5. **Самоанализ** - какие навыки я применяю

### Безопасная среда:
- Можно пробовать разные варианты
- Нет наказания за "неправильный" выбор
- Constructive feedback
- Поощрение к повторной попытке

## 🔧 Технические детали

### Зависимости:
```typescript
import { motion, AnimatePresence } from 'framer-motion';
import { CheckCircle, XCircle, AlertCircle, Lightbulb, TrendingUp } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Card, CardContent } from '@/components/ui/card';
```

### State:
```typescript
const [selectedChoice, setSelectedChoice] = useState<string | null>(null);
const [showResult, setShowResult] = useState(false);
```

### Стили:
- Glassmorphism: `bg-white/60 backdrop-blur-[20px]`
- iOS shadows: `shadow-ios-soft`
- Rounded: `rounded-2xl` до `rounded-3xl`
- Border: `border-white/20`

## 📊 Первый сценарий (в уроке boundaries-w1-1)

**Ситуация:** Друг приходит без предупреждения

**Варианты:**
1. ❌ Промолчать и злиться → пассивное поведение
2. ✅ Честно сказать с предложением → здоровая граница
3. ❌ Накричать → агрессивное поведение
4. ✅ С эмпатией + граница + решение → идеальный вариант

**XP reward:** 60 (как за обычный вопрос средней сложности)

## 🎯 Будущие сценарии

### Для других уроков можно создать:
1. **Эмоциональные границы:**
   - Родитель читает твой дневник
   - Друг делится чужими секретами
   
2. **Временные границы:**
   - Просят помочь, но у тебя экзамен
   - Одноклассник списывает постоянно

3. **Цифровые границы:**
   - Требуют ответить немедленно в чате
   - Публикуют твои фото без разрешения

4. **Романтические отношения:**
   - Партнёр ревнует к друзьям
   - Просят показать телефон

## 📈 Аналитика (для бэкенда)

### Что можно отслеживать:
```typescript
{
  scenarioId: 'boundaries-w1-1-q8',
  selectedChoice: 'choice-2',
  isHealthy: true,
  timeSpent: 45, // секунд
  retries: 0, // сколько раз пробовал
  timestamp: Date.now()
}
```

**Инсайты:**
- Какие сценарии труднее всего
- Сколько попыток в среднем
- Какие навыки чаще упоминаются

## 🚀 Интеграция

### В EnhancedLessonInterface:
```typescript
{question.type === 'real-world-scenario' && (
  <RealWorldScenario 
    title={question.title || question.question}
    context={question.context || ''}
    situation={question.situation || ''}
    choices={question.scenarioChoices || []}
    onComplete={(data) => {
      handleAnswer(data);
      setAnswers(prev => ({ ...prev, [question.id]: data }));
    }}
  />
)}
```

### Новый тип вопроса:
```typescript
export type QuestionType = ... | 'real-world-scenario';
```

---

**Статус:** ✅ Полностью реализовано и протестировано  
**Билд:** Успешен (5.68s)  
**Линий кода:** 250+ с анимациями  
**Первый урок:** Теперь содержит 8 вопросов (160 XP total)
