# 🎭 AnimatedKatya v2.0 - Документация

## 🌟 Обзор

AnimatedKatya v2.0 - это супер-анимированный виртуальный психолог для MyTeens.Space с профессиональными анимациями уровня Rive, Sora и Duolingo.

## ✨ Основные возможности

### 1. **Живые биологические эффекты**
- 🫁 **Breathing** - реалистичное дыхание (scale + opacity)
- 👁️ **Blinking** - моргание глаз каждые 3 секунды
- 🌊 **Idle micro-movements** - естественные покачивания

### 2. **3D интерактивность**
- 🖱️ **Mouse parallax** - многослойная 3D реакция на движение мыши
  - rotateX: ±20 градусов
  - rotateY: ±20 градусов
  - Spring physics: stiffness 150, damping 30
- 🤏 **Drag & Drop** - можно перетаскивать с физикой инерции
- ✋ **Hover effects** - увеличение scale 1.1 с spring анимацией
- 👆 **Tap response** - squash effect (scale 0.95)

### 3. **Particle системы**

#### Default mood:
- ✨ 3x sparkles (float type)
- Легкое зеленое свечение

#### Celebrate mood:
- 🎉 4x confetti (burst type)
- ⭐ 4x stars (float type)
- 🌟 3x big stars (orbit type)
- Золотое свечение высокой интенсивности

#### Support mood:
- 💗 5x hearts (float type)
- 💕 3x small hearts (orbit type)
- Розовое теплое свечение

#### Thinking mood:
- 💭 3x thought clouds (orbit type)
- ❓ 2x question marks (float type)
- Синее свечение

### 4. **Динамические настроения**

```typescript
type KatyaMood = 'default' | 'celebrate' | 'thinking' | 'support';
```

Каждое настроение имеет:
- Уникальную цветовую схему
- Свой набор частиц
- Специальные анимации
- Glow эффект

### 5. **Сообщения с анимацией**

- 💬 **Bubble animation** - плавающее облачко с сообщением
- ⌨️ **Typing effect** - буквы появляются по очереди
- 🌈 **Dynamic coloring** - цвет границы меняется под настроение
- ⬆️ **Float animation** - легкое покачивание

### 6. **Визуальные эффекты**

- 🌟 **Glow/Bloom** - динамическое свечение с пульсацией
- 👤 **Dynamic shadow** - тень реагирует на положение мыши
- ⚡ **Filter transitions** - brightness/saturate анимации
- 🎨 **Color morphing** - плавная смена цветов

## 📦 Использование

### Базовый пример:

```tsx
import AnimatedKatyaV2 from '@/components/AnimatedKatyaV2';

<AnimatedKatyaV2 
  mood="default"
  message="Привет!"
  className="w-32 h-32"
  animate={true}
/>
```

### Смена настроений:

```tsx
// Празднование
<AnimatedKatyaV2 mood="celebrate" message="Отлично! 🎉" />

// Поддержка
<AnimatedKatyaV2 mood="support" message="Я верю в тебя! 💗" />

// Размышление
<AnimatedKatyaV2 mood="thinking" message="Хм, интересно..." />

// По умолчанию
<AnimatedKatyaV2 mood="default" message="Начнем?" />
```

### Props

| Prop | Type | Default | Описание |
|------|------|---------|----------|
| `mood` | `KatyaMood` | `'default'` | Настроение персонажа |
| `message` | `string?` | - | Текст сообщения |
| `className` | `string?` | `''` | CSS классы |
| `animate` | `boolean?` | `true` | Включить анимации |

## 🎨 Технические детали

### Анимационные библиотеки:
- **Framer Motion** - основная анимационная система
- **useSpring** - физические spring анимации
- **useTransform** - трансформации на основе mouse движений
- **AnimatePresence** - появление/исчезновение элементов

### Типы particle анимаций:

1. **Float** - плавающие частицы
   - Движение по кривой
   - Вращение
   - Fade in/out

2. **Orbit** - орбитальное движение
   - Круговая траектория
   - Синхронизированное вращение
   - Smooth transitions

3. **Burst** - взрывные частицы
   - Радиальное расширение
   - Быстрое вращение
   - Dramatic scale

### Performance оптимизации:

- ✅ Использование `will-change` для GPU acceleration
- ✅ Spring анимации вместо duration для плавности
- ✅ Conditional rendering частиц
- ✅ Lazy loading изображений
- ✅ Pointer-events: none для overlay элементов

## 🎯 Интеграция с Telegram

AnimatedKatya v2.0 полностью интегрирована с Telegram Web App:

```tsx
import { useTelegram } from '@/hooks/useTelegram';

const { user, haptic } = useTelegram();

<AnimatedKatyaV2 
  mood="celebrate"
  message={`Привет, ${user?.first_name}!`}
  onClick={() => haptic.success()}
/>
```

## 🚀 Планы развития

- [ ] Добавить voice синтез через ElevenLabs
- [ ] Lip sync с речью
- [ ] Больше настроений (angry, confused, excited)
- [ ] AR режим через WebXR
- [ ] Gesture recognition
- [ ] Eye tracking

## 📝 Примечания

- Оптимально работает на размерах 80-200px
- Рекомендуется включать animate={true} для полного опыта
- Частицы автоматически адаптируются под настроение
- Можно комбинировать несколько Катей на странице

## 💡 Best Practices

1. **Используйте правильное настроение:**
   - `celebrate` - после успехов, достижений
   - `support` - при ошибках, поддержке
   - `thinking` - во время размышлений, ожидания
   - `default` - в обычных ситуациях

2. **Сообщения:**
   - Держите текст коротким (до 50 символов)
   - Используйте эмодзи для яркости
   - Персонализируйте через имя пользователя

3. **Размеры:**
   - Маленькие (64-80px) - в списках, карточках
   - Средние (96-128px) - основной контент
   - Большие (160-200px) - герои, акценты

## 🎨 Цветовая палитра

```css
/* Default - Green */
--katya-default: hsl(88 50% 53%);
--katya-default-glow: rgba(120, 200, 80, 0.4);

/* Celebrate - Gold */
--katya-celebrate: hsl(45 100% 51%);
--katya-celebrate-glow: rgba(255, 215, 0, 0.6);

/* Support - Pink */
--katya-support: hsl(0 70% 60%);
--katya-support-glow: rgba(255, 120, 150, 0.5);

/* Thinking - Blue */
--katya-thinking: hsl(200 92% 60%);
--katya-thinking-glow: rgba(100, 180, 255, 0.4);
```

---

Создано с ❤️ для MyTeens.Space
