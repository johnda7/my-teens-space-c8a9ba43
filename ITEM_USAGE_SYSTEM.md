# 🎒 Система использования предметов из магазина

## Дата обновления: 17 ноября 2025

### ✅ Реализовано

#### 1. Компонент Inventory (`Inventory.tsx`)

**Функционал:**
- 📦 Отображение всех купленных предметов
- 🔢 Счётчики количества каждого предмета
- ✨ Использование предметов с конфетти-эффектом
- 📊 Панель активных эффектов
- 🎨 Градиентные карточки для каждого типа предмета

**Предметы в инвентаре:**

1. **⚡ Энергетик** (`energy_boost`)
   - Эффект: +50 энергии
   - Цвет: желто-оранжевый
   - Использование: мгновенное

2. **⏰ Удвоитель XP** (`xp_booster`)
   - Эффект: ×2 XP на следующий урок
   - Цвет: фиолетово-розовый
   - Использование: активируется до урока

3. **🔥 Защита стрика** (`streak_shield`)
   - Эффект: стрик не сбросится если пропустить день
   - Цвет: оранжево-красный
   - Использование: активна до использования

4. **💡 Подсказка** (`hint_pack`)
   - Эффект: показывает правильный ответ в уроке
   - Цвет: сине-голубой
   - Использование: доступна во время урока

5. **⚡⚡ Мега-энергия** (`mega_energy`)
   - Эффект: +100 энергии (полное восстановление)
   - Цвет: ярко-оранжевый
   - Использование: мгновенное

---

#### 2. Логика использования (`handleUseItem` в Index.tsx)

**Энергия (`energy`):**
```typescript
const currentEnergy = parseInt(localStorage.getItem('userEnergy') || '100');
const newEnergy = Math.min(100, currentEnergy + effect.value);
localStorage.setItem('userEnergy', newEnergy.toString());
localStorage.setItem('lastEnergyUpdate', Date.now().toString());
```

**XP Бустер (`xp_boost`):**
```typescript
// Активация
localStorage.setItem('activeXPBoost', 'true');

// При завершении урока
let finalXP = xpEarned;
if (localStorage.getItem('activeXPBoost') === 'true') {
  finalXP = xpEarned * 2;
  localStorage.removeItem('activeXPBoost'); // Сбрасывается после 1 урока
}
```

**Защита стрика (`streak_protection`):**
```typescript
localStorage.setItem('streakProtection', 'true');
localStorage.setItem('streakProtectionDate', new Date().toISOString());
// TODO: Реализовать проверку при проверке стрика
```

**Подсказки (`hint`):**
```typescript
// Хранятся в инвентаре, используются в EnhancedLessonInterface
const hintCount = inventory.getItemCount('hint_pack');
// TODO: Добавить кнопку "Подсказка" в интерфейс урока
```

---

#### 3. Компонент ActiveEffects (`ActiveEffects.tsx`)

**Отображает активные эффекты:**
- 🚀 **×2 XP** - фиолетовый badge с пульсацией
- 🛡️ **Защита** - оранжевый badge
- 💡 **×N подсказок** - голубой badge

**Позиция:** Под header с энергией и валютой

**Анимации:**
- Появление: scale + opacity
- Пульсация иконок
- Исчезновение при деактивации

---

#### 4. Интеграция в Index.tsx

**Новая кнопка "Рюкзак":**
```tsx
<motion.button
  onClick={() => setShowInventory(true)}
  className="relative bg-gradient-to-br from-purple-500 to-pink-500 p-3 rounded-2xl"
>
  <span className="text-2xl">🎒</span>
  {/* Badge с количеством предметов */}
  <div className="absolute -top-1 -right-1 bg-red-500 text-white">
    {totalItems}
  </div>
</motion.button>
```

**Обновлённый LessonComplete:**
- Показывает сообщение "🚀 XP удвоен бустером!"
- Отображает количество полученных монет
- Анимации для бонусов

---

### 📊 LocalStorage структура

```typescript
// Энергия
localStorage.getItem('userEnergy') // '100'
localStorage.getItem('lastEnergyUpdate') // timestamp

// Активные эффекты
localStorage.getItem('activeXPBoost') // 'true' | null
localStorage.getItem('streakProtection') // 'true' | null
localStorage.getItem('streakProtectionDate') // ISO date string

// Инвентарь
localStorage.getItem('userInventory') 
// JSON: { 'energy_boost': 2, 'xp_booster': 1, 'hint_pack': 3 }
```

---

### 🎮 User Flow

**1. Покупка предмета:**
```
Магазин → Кнопка "Купить" → Confetti → Предмет в инвентарь
```

**2. Использование предмета:**
```
Рюкзак → Выбор предмета → Кнопка "Использовать" → Confetti → Эффект применён
```

**3. XP Бустер:**
```
Рюкзак → "Активировать" → Badge "×2 XP" → Урок → XP × 2 → Badge исчезает
```

**4. Энергия:**
```
Рюкзак → "Использовать" → Энергия +50 → Обновление header
```

---

### 🚀 Следующие шаги (TODO)

#### Высокий приоритет:
- [ ] **Подсказки в уроках** - добавить кнопку "💡 Подсказка" в EnhancedLessonInterface
- [ ] **Проверка защиты стрика** - при расчёте streak проверять `streakProtection`
- [ ] **Анимация использования** - при активации предмета показывать эффект на экране

#### Средний приоритет:
- [ ] **Таймеры эффектов** - показывать "действует ещё N часов"
- [ ] **История использования** - логировать когда и что использовано
- [ ] **Лимиты** - максимум 1 XP бустер активен одновременно

#### Низкий приоритет:
- [ ] **Автоматическое использование** - опция "использовать при необходимости"
- [ ] **Подарки** - возможность подарить предмет другу
- [ ] **Комбо эффекты** - бонус при использовании нескольких предметов

---

### 🧪 Тестирование

**Как протестировать:**

1. **Покупка и использование:**
```javascript
// В console браузера:
localStorage.setItem('userCoins', '1000');
localStorage.setItem('userGems', '50');
// Затем купить предметы в магазине
```

2. **Проверка инвентаря:**
```javascript
console.log(JSON.parse(localStorage.getItem('userInventory')));
```

3. **Тест XP бустера:**
```javascript
localStorage.setItem('activeXPBoost', 'true');
// Пройти урок - XP должен удвоиться
```

4. **Тест энергии:**
```javascript
localStorage.setItem('userEnergy', '30');
// Использовать энергетик - должно стать 80
```

---

### 📝 Известные ограничения

1. **Подсказки** - пока не интегрированы в интерфейс урока
2. **Защита стрика** - логика проверки не реализована
3. **Множественные эффекты** - нельзя активировать 2 XP бустера
4. **Таймеры** - нет ограничения по времени действия
5. **Уведомления** - нет push-уведомлений о окончании эффекта

---

### 🎯 Баланс экономики

**Стоимость vs Эффект:**

| Предмет | Цена | Эффект | ROI |
|---------|------|--------|-----|
| Энергетик | 100🪙 | +50 ⚡ | 5 уроков |
| XP Бустер | 200🪙 | ×2 XP | ~100-200 XP |
| Защита стрика | 300🪙 | 1 день | Страховка |
| Подсказка | 150🪙 | 3 шт | Помощь |
| Мега-энергия | 5💎 | +100 ⚡ | Premium |

**Заработок:**
- 1 урок = 10-20🪙 + 50-100 XP
- 1 квест = 50-100🪙 или 5💎
- 1 мини-игра = 50-100🪙 + 50 XP

**Вывод:** Баланс позволяет купить 1-2 бустера за день активной игры.

---

### 📱 Telegram интеграция

**Haptic Feedback:**
- Покупка: `medium`
- Использование: `medium`
- Подсказка: `light`
- Ошибка: `heavy`

**Будущее:**
- Push уведомления "Твой XP бустер активен!"
- Haptic при активации эффекта
- Вибрация при окончании действия

---

**Автор:** AI Agent (Sonnet 4.0)  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию (кроме подсказок и защиты стрика)
