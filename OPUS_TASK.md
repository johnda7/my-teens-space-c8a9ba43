# 📋 Задание для Opus: Модуль обучения родителей

## ✅ Что уже сделано (Sonnet):

### Техническая инфраструктура:
1. ✅ Telegram WebApp SDK интегрирован (`@twa-dev/sdk` в main.tsx)
2. ✅ Хук `useTelegram.ts` - работа с Telegram API (haptic, buttons, user info)
3. ✅ Роль `parent_learning` добавлена в `backend/models.py`
4. ✅ `backend/telegram_auth.py` - валидация HMAC от Telegram
5. ✅ Endpoint `/api/auth/telegram-login` - аутентификация через Telegram
6. ✅ `RoleSelection.tsx` - выбор роли при входе (4 варианта)
7. ✅ `App.tsx` - роутинг с ProtectedRoute (DEV режим включен)

### Текущая структура:
- **Student Dashboard** (`Index.tsx`) - 44 урока, 4 модуля ✅
- **Parent Dashboard** (`ParentDashboard.tsx`) - мониторинг ребенка ✅
- **Curator Dashboard** (`CuratorDashboard.tsx`) - управление группой ✅

---

## ❌ Что НУЖНО спроектировать (Opus):

### 1. 📚 Родительские уроки (`parentLessonsData.ts`)

**Задача:** Создать 20-30 уроков для родителей по структуре аналогичной `allLessonsData.ts`

**Темы уроков:**
- 🗣️ Как говорить с подростком (активное слушание, я-сообщения, эмпатия)
- 💚 Поддержка эмоций (понимание подростковых эмоций, как не обесценивать чувства)
- 🛡️ Установка здоровых границ (баланс контроля и свободы)
- ⚡ Разрешение конфликтов (техники деэскалации, компромиссы)
- 🧠 Понимание подросткового возраста (физиология, психология)
- 🤝 Построение доверия (как восстановить доверие, секреты vs приватность)

**Структура урока (как в `allLessonsData.ts`):**
```typescript
{
  id: 'parent-communication-w1-1',
  title: 'Активное слушание',
  description: 'Учимся слышать подростка',
  module: 'parent-communication',
  week: 1,
  xp: 50,
  questions: [
    {
      type: 'choice' | 'input' | 'slider' | 'multiple',
      question: '...',
      options: [...],
      correctAnswer: '...',
      explanation: '...',
      katyaResponse: '...'
    }
  ],
  completionMessage: '...'
}
```

**Модули для родителей (предложение):**
1. **communication** (общение) - 8 уроков
2. **emotional-support** (эмоц. поддержка) - 7 уроков
3. **boundaries-parenting** (границы в воспитании) - 8 уроков
4. **conflict-resolution** (конфликты) - 7 уроков

---

### 2. 🏠 ParentHub.tsx - Центральный дашборд родителя

**Задача:** Создать страницу с двумя табами/секциями

**Секция 1: Мониторинг детей**
- Использовать существующий `ParentDashboard.tsx` компонент
- Показать прогресс ребенка, balance wheel, статистику
- Кнопка "Добавить ребенка" → открывает `AddChild.tsx`

**Секция 2: Мое обучение**
- Карточки модулей для родителей (аналогично Index.tsx)
- Прогресс по урокам родителя
- XP, level, streak родителя
- Переход к урокам → `ParentLearning.tsx`

**UI структура:**
```tsx
<ParentHub>
  <Tabs>
    <Tab label="Мои дети 👨‍👩‍👧">
      <ChildrenMonitoring />
      <AddChildButton />
    </Tab>
    <Tab label="Мое обучение 📚">
      <ParentModules />
      <ParentProgress />
    </Tab>
  </Tabs>
</ParentHub>
```

---

### 3. 📖 ParentLearning.tsx - Интерфейс уроков для родителей

**Задача:** Адаптировать `Index.tsx` для родительских уроков

**Отличия от студенческого:**
- Использует `parentLessonsData.ts` вместо `allLessonsData.ts`
- Другие модули (communication, emotional-support, etc)
- Тон более взрослый, но дружелюбный
- Катя Карпенко общается с родителем (советы, поддержка)

**Что можно переиспользовать:**
- `EnhancedLessonInterface.tsx` - работает с любыми уроками ✅
- `ModuleRoom.tsx` - можно адаптировать
- `LessonComplete.tsx` - универсальный ✅
- State management из Index.tsx

---

### 4. 👨‍👩‍👧 AddChild.tsx - Форма связывания родитель-ребенок

**Задача:** Создать компонент для ввода 6-значного кода ребенка

**Функционал:**
- Input для кода (6 символов)
- Валидация кода через API
- POST `/api/family/link-parent` с кодом
- Показ списка уже привязанных детей
- Возможность отвязать ребенка

**UI:**
```tsx
<AddChild>
  <Input 
    placeholder="Введите код ребенка (6 символов)"
    maxLength={6}
  />
  <Button onClick={handleLink}>Привязать</Button>
  
  <LinkedChildrenList>
    {children.map(child => (
      <ChildCard>
        <Name>{child.name}</Name>
        <UnlinkButton />
      </ChildCard>
    ))}
  </LinkedChildrenList>
</AddChild>
```

---

### 5. 🔗 Family Links API Endpoints

**Задача:** Добавить в `backend/server.py` endpoints для связывания

**Endpoints:**

```python
@api_router.post("/family/generate-link-code")
async def generate_family_link_code(student_id: str):
    """
    Генерирует 6-значный код для студента
    Родитель использует этот код для привязки
    """
    code = generate_6_digit_code()
    expires_at = datetime.utcnow() + timedelta(days=7)
    
    await db.family_links.insert_one({
        "code": code,
        "student_id": student_id,
        "used": False,
        "created_at": datetime.utcnow(),
        "expires_at": expires_at
    })
    
    return {"code": code, "expires_at": expires_at}

@api_router.post("/family/link-parent")
async def link_parent_to_child(parent_id: str, code: str):
    """
    Привязывает родителя к ребенку по коду
    """
    link = await db.family_links.find_one({
        "code": code.upper(),
        "used": False,
        "expires_at": {"$gt": datetime.utcnow()}
    })
    
    if not link:
        raise HTTPException(404, "Неверный или истекший код")
    
    # Обновляем связь
    await db.users.update_one(
        {"id": link["student_id"]},
        {"$set": {"parent_id": parent_id}}
    )
    
    await db.family_links.update_one(
        {"code": code},
        {"$set": {"used": True, "parent_id": parent_id}}
    )
    
    return {"message": "Ребенок успешно привязан!"}

@api_router.get("/family/{user_id}/links")
async def get_family_links(user_id: str):
    """
    Получить список детей родителя или код студента
    """
    user = await db.users.find_one({"id": user_id})
    
    if user["role"] == "parent":
        # Получить детей
        children = await db.users.find({"parent_id": user_id}).to_list(100)
        return {"children": children}
    elif user["role"] == "student":
        # Получить активный код
        link = await db.family_links.find_one({
            "student_id": user_id,
            "used": False,
            "expires_at": {"$gt": datetime.utcnow()}
        })
        return {"link_code": link}
```

---

### 6. 🎨 Кнопка "Пригласить родителя" в Index.tsx

**Задача:** Добавить в профиль студента секцию с генерацией кода

**Где добавить:** В `Index.tsx` в секции Profile/Settings

**UI:**
```tsx
<ProfileSection>
  <h3>Пригласить родителя</h3>
  <p>Поделитесь кодом с родителем для мониторинга прогресса</p>
  
  {!familyCode ? (
    <Button onClick={generateCode}>
      Создать код приглашения
    </Button>
  ) : (
    <Card>
      <h4>Ваш код:</h4>
      <Code>{familyCode}</Code>
      <p>Действителен 7 дней</p>
      <CopyButton />
    </Card>
  )}
</ProfileSection>
```

---

## 🎯 Приоритеты:

1. **Высокий:** `parentLessonsData.ts` - контент критичен
2. **Высокий:** `ParentHub.tsx` - центральная точка для родителей
3. **Средний:** `ParentLearning.tsx` - переиспользуем много из Index.tsx
4. **Средний:** Family Links API - техническая задача
5. **Низкий:** `AddChild.tsx` - простая форма
6. **Низкий:** Кнопка в Index.tsx - небольшое дополнение

---

## 📝 Инструкции для Sonnet (после проектирования):

После того как Opus предоставит:
1. Структуру `parentLessonsData.ts` с уроками
2. Дизайн компонентов ParentHub и ParentLearning
3. Структуру API endpoints

Sonnet должен:
1. Изучить предоставленные файлы/код
2. Создать файлы согласно дизайну
3. Интегрировать с существующей инфраструктурой
4. Протестировать локально
5. Закоммитить изменения

---

## 🔄 Workflow:

```
Opus (design) → код/структура → Sonnet (implement) → тестирование → деплой
```

**Текущий статус:** Ожидаем дизайн от Opus для родительского модуля обучения.

---

**Дата:** 10 ноября 2025
**Автор задания:** Sonnet (реализатор)
**Для:** Opus (проектировщик)
