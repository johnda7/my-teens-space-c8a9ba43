# ⚡ Быстрый деплой на GitHub Pages

## ✅ Что уже сделано:

1. ✅ Настроен `vite.config.ts` для GitHub Pages
2. ✅ Обновлён GitHub Actions workflow (`.github/workflows/deploy.yml`)
3. ✅ Добавлен `404.html` для SPA роутинга
4. ✅ Создана полная инструкция в `GITHUB_PAGES_SETUP.md`

## 🚀 Что нужно сделать СЕЙЧАС:

### Вариант 1: С кастомным доменом (например, myteens.space)

1. **Создайте файл** `frontend/public/CNAME` с содержимым:
   ```
   myteens.space
   ```
   (или ваш домен)

2. **Настройте DNS** у вашего регистратора:
   - Для основного домена: добавьте A-записи на IP GitHub
   - Для поддомена: добавьте CNAME на `johnda7.github.io`
   
   Подробности в `GITHUB_PAGES_SETUP.md`

3. **Запушьте изменения:**
   ```bash
   git add .
   git commit -m "Setup GitHub Pages with custom domain"
   git push origin main
   ```

4. **В GitHub Settings → Pages:**
   - Укажите ваш домен в поле "Custom domain"
   - Включите "Enforce HTTPS" после проверки

### Вариант 2: Без кастомного домена (стандартный GitHub Pages)

1. **Обновите** `frontend/vite.config.ts`, строка 10:
   ```typescript
   base: '/my-teens-space-c8a9ba43/',
   ```

2. **Запушьте изменения:**
   ```bash
   git add .
   git commit -m "Setup GitHub Pages"
   git push origin main
   ```

3. **В GitHub Settings → Pages:**
   - Убедитесь, что Source = "GitHub Actions"

## 📍 После деплоя:

Ваш сайт будет доступен по адресу:
- **С кастомным доменом:** `https://myteens.space`
- **Без кастомного домена:** `https://johnda7.github.io/my-teens-space-c8a9ba43`

## ⚠️ ВАЖНО: Бэкенд

GitHub Pages хостит только фронтенд. Для работы приложения нужно:

1. Развернуть бэкенд отдельно (Railway, Render, Heroku)
2. Добавить URL бэкенда в GitHub Secrets:
   - Settings → Secrets → Actions → New secret
   - Name: `VITE_API_URL`
   - Value: `https://your-backend-url.com/api`

## 🎯 Следующие шаги:

1. Определитесь с доменом
2. Следуйте варианту 1 или 2 выше
3. Проверьте деплой в GitHub Actions
4. Настройте бэкенд отдельно

---

**Нужна помощь?** Смотрите полную инструкцию в `GITHUB_PAGES_SETUP.md`
